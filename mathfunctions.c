#include<stdio.h>
#include<math.h> //this stores math 

int main(){

    double A = sqrt(9);
    double B = pow(2, 4);
    int C = round(3.14);
    int D = ceil(3.14); //rounding up 
    int E = floor(3.99); //round down
    double F = fabs(-100); //absolute value
    double G = log(3); //logarithm of 3
    double H = sin(45);  
    double I = cos(45);
    double J = tan(45);

    //printf("\nlf",B);

    //printf("\n%d",  C  );

    printf("\nlf",F);

}